import 'package:flutter/material.dart';

class BrosweScreen extends StatelessWidget {
  const BrosweScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}